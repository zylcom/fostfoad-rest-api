{
  "buildCommand": "tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
  "builds": [{ "src": "api/index.js", "use": "@vercel/node" }],
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
